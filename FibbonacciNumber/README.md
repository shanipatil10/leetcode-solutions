This solution uses a simple recursive approach where each call computes
fib(n) = fib(n-1) + fib(n-2).
It is easy to understand but has exponential time complexity O(2ⁿ) and is inefficient for large n.
Use this only for learning or small inputs.
